


      fluttergen -c pubspec.yaml;fvm flutter pub run easy_localization:generate -S assets/translations -f keys -O lib/gen -o locale_keys.g.dart

      
      flutter pub cache clean
      fluttergen -c pubspec.yaml 


      ./gradlew signingReport

      git config --global user.email ao25332@gmail.com ; git config --global user.name Abdallah Kddah
      git add .;
      git commit -m "fix work hour and days in vendor details"
      ;git push;
      fvm flutter clean

      fvm flutter clean;fvm flutter pub get;fvm flutter build apk --release

      git push origin stg:main

      git reset --hard commit_hash_you_want_to_return_to

      git checkout stg;git merge main;git push;fvm flutter clean ;fvm flutter pub get ;fvm flutter build apk;git checkout main


      git add .;
      git commit -m "first commit"
      ;git push;git checkout main;git merge dev;git push;git checkout dev

      app links:


git cherry-pick -e <commit-hash>
                                                                                                         
git init;
git add README.md;
git commit -m "first commit";
git branch -M main;
git remote add origin https://github.com/akddah/octopii_provider.git;
git push -u origin main;


fvm flutter pub run build_runner build --delete-conflicting-outputs


git stash        
git stash pop        

//